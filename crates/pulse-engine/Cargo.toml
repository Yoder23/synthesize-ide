[package]
name = "pulse-engine"
version = "0.1.0"
edition = "2021"

[features]
default = []
liquid-observer = []

[dependencies]
intent-ledger = { path = "../intent-ledger" }
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
hex.workspace = true
thiserror.workspace = true

