[package]
name = "worktree-manager"
version = "0.1.0"
edition = "2021"

[dependencies]
audit-log = { path = "../audit-log" }
intent-ledger = { path = "../intent-ledger" }
repo-guard = { path = "../repo-guard" }
rusqlite.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
